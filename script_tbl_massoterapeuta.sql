CREATE TABLE IF NOT EXISTS `db_turismo`.`tbl_massoterapeuta` (
	`id` INT NOT NULL AUTO_INCREMENT,
    `nome` VARCHAR(255),
    `qualidade_da_massagem` VARCHAR(255),
    `preco` VARCHAR(255),
    PRIMARY KEY(`id`))